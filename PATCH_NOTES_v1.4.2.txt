AxetosOS Products / NES v1.4.2

Goal: recover the v1.4.1 performance regression and reduce steady-state package-entry overhead without changing the physical hardware boundary.

Main sweep:
  - remove v1.4.1 incremental multi-driver state bookkeeping
  - resolve common 3/4-driver shared traces directly from physical package-pin state
  - retain package-boundary atomic publication without a duplicate software driver-state cache
  - keep v1.4.1 chip-gated pin storage and fixed-width DigitalBus fast paths
  - add exact clock-only steady-state paths inside NTSC/PAL CPU and PPU packages
  - remove obsolete per-pin compiled driver index
  - add four-driver electrical regression coverage

Architecture unchanged:
  - motherboard knows only topology/electrical resolution
  - every physical level is still delivered/stored
  - chip pins own wake/edge/divider semantics
  - no queue, scheduler, Settle engine, receiver-aware suppression, or skipped clock pulse

Run dotnet test before Release benchmarks.
Expected test total after this patch: 215.
