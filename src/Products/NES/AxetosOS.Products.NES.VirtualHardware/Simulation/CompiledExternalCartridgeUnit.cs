namespace AxetosOS.Products.NES.VirtualHardware.Simulation;

// Retired in v2.12.0. Replaceable devices now expose the same product-agnostic
// compiler facets as every other hardware component and are marked only with
// ICompiledExternalDevice so the fixed-board runtime can keep them as separate
// execution units.
