AxetosOS Products / NES v1.0.2

Correction
- Removes the final stale test call to the deleted Pulse(..., settlePasses:) compatibility API.
- Uses the current Pulse(DigitalSignalSource, VirtualHardwareSimulator) helper.
- Does not restore a compatibility overload or any removed runtime code.

Application
1. Extract this ZIP over C:\Users\patri\source\repos.
2. From AxetosOS.Products.NES, run dotnet test.

Validation performed here
- Confirmed that settlePasses no longer occurs anywhere in the reconstructed v1.0.2 source tree.
- Confirmed that the changed test call matches the existing two-argument Pulse helper.
- The .NET SDK is unavailable in this environment, so dotnet test was not run here.
