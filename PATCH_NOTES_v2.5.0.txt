AxetosOS Products / NES v2.5.0

Scope: aggressive generic true-hardware package/electrical hot path.

Intentional baseline:
- restore validated v2.2 behavior
- discard v2.3 destination-package aggregation
- discard v2.4 HM6116/74-series retained-input experiment

Aggressive changes:
- 64-bit package output change mask replaces changed-pin reference array/stamps
- package-local parallel 6/8/16-pin bus staging
- one-byte packed pin drive level + strength
- packed 2/3/4/multi-driver net resolution
- byte-sized electrical enums

Hardware semantics remain unchanged:
- every changed physical output pin is published through its actual net
- every net resolves independently
- every connected package pin retains the resolved physical level
- strong/weak, Hi-Z, unknown and contention remain physical
- bidirectional self-drive isolation remains
- package output reactions remain atomic
- chips gain no knowledge of boards, wires or peer chips

Expected tests: 228 total.
