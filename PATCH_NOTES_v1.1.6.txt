AxetosOS Products / NES v1.1.6

Purpose
-------
Correct the sole remaining v1.1.5 test failure. The runtime correctly resolved two opposing strong drivers as Contention; the regression test incorrectly expected Low.

Changes
-------
- Changes the external source in Releasing_bidirectional_pin_does_not_invent_an_input_transition_when_external_level_is_unchanged from High to Low.
- The package also drives Low, so the physical bus remains Low while driven and after release.
- The test now measures only the intended invariant: releasing onto an unchanged externally driven level must not manufacture an input transition.
- No runtime source code is changed.

Architecture
------------
- Opposing strong drivers still resolve to Contention.
- Chips still execute only from incoming transitions on input-capable package pins.
- No polling, scheduler, lifecycle callback, peer-chip reference, or board shortcut is restored.

Validation
----------
The user reported v1.1.5 at 366 passed / 1 failed. The only failure was this test's electrically invalid Low expectation under High-vs-Low contention. This package corrects only that test and version metadata. dotnet is not available in the packaging environment, so the target machine remains the execution authority.
