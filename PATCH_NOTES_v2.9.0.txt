v2.9.0 clean transport replacement experiment

Apply over v2.8.0. This patch restores the v2.6 host/board/chip baseline where v2.8 changed it, overwrites the v2.8 generic compiler source with the new package-output transport, and makes that transport the sole steady-state package-output fan-out after topology compilation.
