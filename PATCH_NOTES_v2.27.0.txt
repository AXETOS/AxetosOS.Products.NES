AxetosOS Products / NES v2.27.0

Performance target:
- validated Alien Syndrome v2.26.1 baseline: 45.77 FPS normal Release/uncapped
- target: reduce generic whole-circuit compiler overhead toward real-time NTSC speed

Implementation:
- bind-time static condition classification for dynamic bus targets
- state-independent replaceable-package combinational facet
- MMC1 /CIRAM-CE proven at bind time; live mapper-controlled CIRAM A10 remains runtime
- fixed local-address projection cached per master address
- empty begin-read and dynamic-pass elimination
- direct single-target complete-read path
- cached target delegates / single observer / IRQ sampler
- RP2A03 write-completion call only after writes
- post-ejection compiled dispatch is rebuilt from the already-detached physical netlist

No hardware transition, PPU fetch, mapper access, or package-owned state change is intentionally skipped.
No FPS result is claimed until measured locally.
Expected test total after the added regression: 280.
