v2.16.3 precise MMC1 raster-correlation trace

Apply over v2.16.2.

Changes:
- use 12-master-clock diagnostic batches only while cartridge-video capture is active;
- retain the normal 16,384-master-clock host batch outside trace capture;
- stamp MMC1 register diagnostics with exact cartridge-local PPU read/write counters;
- print explicit PPU-read drain lag beside the near-cycle raster observation;
- delay CART FRAME emission until the RP2C0x frame counter advances so pre-render fetches stay in one summary;
- no hardware or compiler execution behavior changes.

Expected test total: 262.
Local dotnet test is the acceptance gate.
