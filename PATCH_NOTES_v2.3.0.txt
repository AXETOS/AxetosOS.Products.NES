AxetosOS Products / NES v2.3.0

Scope: generic true-hardware package-boundary performance.

Changed runtime representation:
- <=64 physical package output changes are staged in a packed bit mask.
- >64-pin packages retain a reusable overflow publication path.
- multi-net destination input masks are merged inside the synchronous propagation frame.
- each destination package reacts once after all changed source-package traces are physically presented.

Physical semantics intentionally unchanged:
- every changed output pin is published
- every attached net resolves independently
- every connected package pin stores the delivered level
- Hi-Z, strength, unknown and contention semantics
- bidirectional self-drive isolation
- chip-owned wake/edge/divider activation
- package-boundary atomicity
- re-entrant chip-local pending input handling

Validated incoming v2.2.0 tests: 226/226.
Expected v2.3.0 tests: 228 total.
