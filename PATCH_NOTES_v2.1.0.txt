AxetosOS Products / NES v2.1.0 patch

Purpose:
- Replace the v2.0 compiled-route experiment with one fused Famicom/NROM runtime circuit.
- Remove ordinary DigitalPin/DigitalNet/decoder/RAM/cartridge package dispatch from the normal compiled hot loop.
- Precompile the fixed 12-master-cycle RP2A03/RP2C02 divider schedule.
- Preserve the existing reference runtime behind --reference-runtime.
- Restore the v1.5.2 electrical core files so reference mode does not pay the v2.0 route-hook overhead.

Expected test result: 224 passed, 0 failed.

Do not push this experimental release to GitHub until local tests and compiled/reference benchmarks are accepted.
